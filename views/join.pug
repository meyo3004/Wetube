extends layouts/main

block content
    .form__container
        form(action=routes.join, method="post") 
            input(type="text", name="name", placeholedr="Full Name")
            input(type="email", name="email", placeholedr="Email")
            input(type="password", name="password", placeholedr="Password")
            input(type="password", name="password2", placeholedr="Verify Password")
            input(type="submit", value="Join Now")
        include partials/socialLogin
