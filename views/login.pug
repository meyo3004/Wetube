extends layouts/main

block content
    .form__container
        form(action=routes.login, method="post") 
            input(type="email", name="email", placeholedr="Email")
            input(type="password", name="password", placeholedr="Password")
            input(type="submit", value="Log In")
        include partials/socialLogin
